name: Docker Image CI

on:
  push:
    branches: [ main ]
    
jobs:

  build:

    runs-on: docker:20.10.12-alpine3.15

    steps:
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag  --tag pyramid
